<UserControl x:Class="ExcelAppCR.View.UserControls.ExcelUser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExcelAppCR.View.UserControls"
             xmlns:converters="clr-namespace:ExcelAppCR.Converters"
              xmlns:vm="clr-namespace:ExcelAppCR.ViewModel"
             mc:Ignorable="d" 
             x:Name="root"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             d:DesignHeight="600" d:DesignWidth="1280">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolVisibility"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolVisibility"/>
        <Style x:Key="ModernLoadingSpinnerStyle" TargetType="ProgressBar">
            <Setter Property="Visibility" Value="Visible"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <StackPanel>
                            <Grid Width="50" Height="50" RenderTransformOrigin="0.5,0.5">

                                <Path Stroke="#3B82F6" StrokeThickness="6" StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                                    <Path.Data>
                                        <PathGeometry>
                                            <PathFigure StartPoint="45,25">
                                                <ArcSegment Size="20,20" Point="25,5" SweepDirection="Clockwise" IsLargeArc="True"/>
                                            </PathFigure>
                                        </PathGeometry>
                                    </Path.Data>
                                </Path>

                                <Grid.RenderTransform>
                                    <RotateTransform Angle="0" />
                                </Grid.RenderTransform>
                            </Grid>

                            <TextBlock Text="Đang tải dữ liệu..."
                                    FontSize="18"
                                    Foreground="#4A5568"
                                    HorizontalAlignment="Center"
                                    Margin="0,25,0,0"/>
                        </StackPanel>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsIndeterminate" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                      From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MainPanelStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentState}" Value="{x:Static vm:ViewState.DataLoaded}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="EmptyPanelStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentState}" Value="{x:Static vm:ViewState.Empty}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LoadingPanelStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentState}" Value="{x:Static vm:ViewState.Loading}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>
    <Grid Margin="0" 
              d:Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">

            <Border Margin="5"
                    BorderThickness="1"
                    BorderBrush="Gray"
                    CornerRadius="10">
                <DataGrid Background="White"
                          BorderBrush="Transparent" 
                           HeadersVisibility="Column"
                          BorderThickness="0"
                          GridLinesVisibility="All"
                          AlternatingRowBackground="#F5F5F5"
                          AutoGenerateColumns="True" 
                          EnableRowVirtualization="True"
                          IsReadOnly="False" 
                         Style="{StaticResource MainPanelStyle}"
                          VirtualizingPanel.IsVirtualizing="True" 
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          ItemsSource="{Binding ExcelData}"
                          Margin="5">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="FontSize" Value="15"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <!-- Cell Style -->
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="5,3"/>
                            <Setter Property="FontSize" Value="15"/>
                            <Setter Property="MinHeight" Value="30"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E8F5E8"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Border>


            <Border Background="White"
                    Style="{StaticResource EmptyPanelStyle}">
                <StackPanel VerticalAlignment="Center" 
                            HorizontalAlignment="Center">
                    <TextBlock Text="📄" 
                               FontSize="48"
                               Foreground="#BDC3C7" 
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="No data loaded yet"
                               FontSize="16" 
                               Foreground="#7F8C8D"
                               Margin="0,10,0,0"
                               HorizontalAlignment="Center"/>

                </StackPanel>
            </Border>

            <Border Background="#80FFFFFF" 
                  Style="{StaticResource LoadingPanelStyle}">
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True"
                                 Style="{StaticResource ModernLoadingSpinnerStyle}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <TextBlock HorizontalAlignment="Left" 
                       Margin="10 0"
                       FontFamily="/Fonts/Nexa.ttf#Nexa"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       FontSize="18">
                     <Run Text="Total Record :"/>
                     <Run Text="{Binding TotalRecords, Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}"/>
            </TextBlock>

            <StackPanel HorizontalAlignment="Right"
                        Margin="10 5 15 5"
                        Orientation="Horizontal">
                <TextBlock Text="📊 Processing Status:" 
                            FontWeight="Bold"
                            FontSize="15"
                            FontFamily="/Fonts/Nexa.ttf#Nexa"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0"/>

                <!--<ProgressBar Height="20" 
                             Margin="0 0 50 0" 
                             VerticalAlignment="Center"
                             Foreground="Green"
                             IsIndeterminate="True"
                             Value="{Binding IsProcessing ,Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}"
                             Background="Azure"
                             Width="200"/>-->
                <TextBlock Text="Page Size :" 
                           FontSize="18"
                           FontWeight="Bold"
                           FontFamily="/Fonts/Nexa.ttf#Nexa"
                           Margin="0 0 10 0"
                           VerticalAlignment="Center"/>
                <ComboBox Width="80" 
                          Height="25"
                          SelectedValue="{Binding PageSize}"
                          VerticalAlignment="Center"
                          Margin="0,0,20,0">
                    <ComboBoxItem Content="100"/>
                    <ComboBoxItem Content="500"/>
                    <ComboBoxItem Content="1000"/>
                    <ComboBoxItem Content="2000"/>
                    <ComboBoxItem Content="5000"/>
                </ComboBox>
                <Button Style="{StaticResource btnNP }"
                        Command="{Binding PreviousPageCommand}"
                        CommandParameter="{Binding ElementName=root}">
                    <Image Source="/Images/backP.png"
                           Margin="5"/>
                </Button>
                <TextBlock 
                        FontWeight="Bold"
                        FontSize="20"
                        VerticalAlignment="Center">
                    <Run Text="{Binding PageIndex , Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}"/>
                    <Run Text="of"/> 
                    <Run Text="{Binding TotalPages , Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                </TextBlock>
                <Button Style="{StaticResource btnNP}"
                        Command="{Binding NextPageCommand }" 
                        CommandParameter="{Binding ElementName=root}">
                    <Image Source="/Images/nextP.png"
                           Margin="5"/>
                </Button>
            </StackPanel>

        </Grid>
    </Grid>


</UserControl>
