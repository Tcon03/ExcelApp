<UserControl x:Class="ExcelApp.View.UserControls.ExcelUser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExcelApp.View.UserControls"
             xmlns:converters="clr-namespace:ExcelApp.Converters"
             xmlns:vm="clr-namespace:ExcelApp.ViewModel"
             mc:Ignorable="d" 
             x:Name="root"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"  
             d:DesignHeight="600" d:DesignWidth="1280">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolVisibility"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolVisibility"/>
        <Style x:Key="CircleStyle" TargetType="ProgressBar">
            <Setter Property="Foreground" Value="#3B82F6"/>
            <Setter Property="IsIndeterminate" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <!-- Vòng quay -->
                            <Grid x:Name="Spinner" Width="50" 
                                  Height="50" 
                                  RenderTransformOrigin="0.5,0.5">
                                <Grid.RenderTransform>
                                    <!--  storyboard bám vào -->
                                    <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                                </Grid.RenderTransform>

                                <!-- Cung tròn (một đoạn của vòng tròn) -->
                                <Path Stroke="{TemplateBinding Foreground}"
                                      StrokeThickness="6"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round">
                                    <Path.Data>
                                        <PathGeometry>
                                            <PathFigure StartPoint="45,25">
                                                <!-- Cung khi quay -->
                                                <ArcSegment Size="20,20"
                                                    Point="25,5"
                                                    IsLargeArc="True"
                                                    SweepDirection="Clockwise"/>
                                            </PathFigure>
                                        </PathGeometry>
                                    </Path.Data>
                                </Path>
                            </Grid>

                            <TextBlock Text="Đang tải dữ liệu..."
                               FontSize="14"
                               Foreground="#4A5568"
                               HorizontalAlignment="Center"
                               Margin="0,12,0,0"/>
                        </StackPanel>

                        <!-- Kích hoạt animation khi indeterminate -->
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsIndeterminate" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard x:Name="SpinStoryboard">
                                        <Storyboard RepeatBehavior="Forever">
                                            <!-- Carget vào Grid và đi qua RenderTransform.Angle -->
                                            <DoubleAnimation
                                        Storyboard.TargetName="Spinner"
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                        From="0" To="360" Duration="0:0:1"/>

                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <StopStoryboard BeginStoryboardName="SpinStoryboard"/>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--LoadDataStyle-->
        <Style x:Key="LoadDataStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" 
                    Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentState}" 
                             Value="{x:Static vm:ViewState.DataLoaded}">
                    <Setter Property="Visibility" 
                            Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!--EmptyStyle-->
        <Style x:Key="EmptyStyle"
               TargetType="FrameworkElement">
            <Setter Property="Visibility"
                    Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentState}" 
                             Value="{x:Static vm:ViewState.Empty}">
                    <Setter Property="Visibility" 
                            Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!--SaveProcess-->
        <Style TargetType="FrameworkElement" x:Key="SaveProcess">
            <Setter Property="Visibility"
                    Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentState}" 
                             Value="{x:Static vm:ViewState.DataSave}">
                    <Setter Property="Visibility" 
                            Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!--LoadingPanelStyle-->
        <Style x:Key="LoadingPanelStyle" 
               TargetType="FrameworkElement">
            <Setter Property="Visibility" 
                    Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentState}" 
                             Value="{x:Static vm:ViewState.Loading}">
                    <Setter Property="Visibility"
                            Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>


    <Grid Margin="0" 
          Background="#FFFFFF">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>


        <!--Item 1-->
        <Grid Grid.Row="0">
            <Border Margin="10"
                    BorderThickness="2"
                    BorderBrush="LightGray"
                    CornerRadius="10">
                <!--dataGrid loaded-->
                <DataGrid Background="White"
                          BorderBrush="Transparent" 
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          GridLinesVisibility="All"
                          AlternatingRowBackground="#F5F5F5"
                          AutoGenerateColumns="True" 
                          EnableRowVirtualization="True"
                          IsReadOnly="False" 
                          Style="{StaticResource LoadDataStyle}"
                          VirtualizingPanel.IsVirtualizing="True" 
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          ItemsSource="{Binding ExcelData}"
                          Margin="5">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="FontSize" Value="15"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <!-- Cell Style -->
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="5,3"/>
                            <Setter Property="Margin" Value="5 0"/>
                            <Setter Property="FontSize" Value="15"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="MinHeight" Value="35"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E8F5E8"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Border>


            <!--Empty panel-->
            <Border Background="White"
                    Style="{StaticResource EmptyStyle}">
                <StackPanel VerticalAlignment="Center" 
                            HorizontalAlignment="Center">
                    <TextBlock Text="📄" 
                               FontSize="70"
                               Foreground="LightGray"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="No data loaded yet"
                               FontSize="20" 
                               Foreground="#212529"
                               FontWeight="DemiBold"
                               Margin="0,10,0,0"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Open a file or create a new one to get started "
                               FontSize="20" 
                               Margin="0 5 0 0"
                               Foreground="Gray"
                               HorizontalAlignment="Center"/>
                    <StackPanel Margin="20">
                        <Button Command="{Binding OpenExcelCommand}"
                                Style="{StaticResource BtnOpen}"
                                CommandParameter="{Binding ElementName=root}">

                            <StackPanel Orientation="Horizontal">
                                <Icon:PackIconBootstrapIcons 
                                        Kind="Folder2Open" 
                                        Width="20" 
                                        Foreground="Yellow"
                                        FontWeight="Bold"
                                        Height="30" 
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,10,0"/>
                                <TextBlock Text="Open Folder"
                                          VerticalAlignment="Center"
                                          Foreground="White"
                                          HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!--Loading panel-->
            <Border Background="#80FFFFFF" 
                    Style="{StaticResource LoadingPanelStyle}">
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True"
                                 Style="{StaticResource CircleStyle}"/>
                </StackPanel>
            </Border>
        </Grid>


        <!--Item 2-->
        <Border Grid.Row="1"
                BorderThickness="1"
                Margin="0 5 0 0"
                BorderBrush="LightGray">
            <Grid>
                <TextBlock HorizontalAlignment="Left" 
                       Margin="10 0"
                       FontFamily="/Fonts/Nexa.ttf#Nexa"
                       FontWeight="Bold"
                       Foreground="#212529"
                       VerticalAlignment="Center"
                       FontSize="18">
                     <Run Text="Total Record :"/>
                     <Run Text="{Binding TotalRecords, Mode=OneWay , UpdateSourceTrigger=PropertyChanged ,StringFormat=N0}"/>
                </TextBlock>

                <StackPanel HorizontalAlignment="Right"
                        Margin="10 5 15 5"
                        Orientation="Horizontal">

                    <TextBlock VerticalAlignment="Center"
                               Margin="0,0,25,0" 
                                FontSize="18">
                          <Run Text="Load Time: "/>
                          <Run Foreground="Green"
                               Text="{Binding LastLoadTime, StringFormat={}{0:F0}ms}" 
                               FontWeight="DemiBold"/>
                    </TextBlock>
                    
                    <Border  Visibility="{Binding IsSaved, Converter={StaticResource BoolVisibility}}">
                        <StackPanel Orientation="Horizontal">


                            <TextBlock  Text="📊 Save File :" 
                                    FontWeight="Bold"
                                    FontSize="15"
                                    FontFamily="/Fonts/Nexa.ttf#Nexa"
                                    VerticalAlignment="Center"
                                    Margin="0,0,10,0"/>

                            <ProgressBar Height="20" 
                                    Margin="0 0 50 0" 
                                    VerticalAlignment="Center"
                                    Foreground="Green"
                                    IsIndeterminate="True"
                                    Background="Azure"
                                    Width="200"/>
                        </StackPanel>
                    </Border>


                    <TextBlock Text="Page Size :" 
                           FontSize="18"
                           FontWeight="DemiBold"
                           Foreground="#212529"
                           FontFamily="Segoe UI"
                           Margin="0 0 15 0"
                           VerticalAlignment="Center"/>
                    <ComboBox Width="80" 
                          Height="25"
                          FontWeight="Bold"
                          Foreground="#212529"
                          ItemsSource="{Binding PageSizeOptions}"
                          IsEnabled="{Binding IsNotProcessing}"
                          SelectedItem="{Binding PageSize,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged }"
                           VerticalContentAlignment="Center"
                          Margin="0,0,20,0">
                    </ComboBox>

                    <Button Style="{StaticResource btnNP }"
                            IsEnabled="{Binding IsNotProcessing}"
                            Command="{Binding PreviousPageCommand}"
                            Margin="0 0 0 -2"
                            CommandParameter="{Binding ElementName=root}">
                        <Icon:PackIconMaterial Kind="ChevronLeft" 
                                               Height="20"
                                                VerticalAlignment="Center"/>
                    </Button>
                    <TextBlock 
                        FontSize="20" 
                        Margin="5 0 5 0"
                        FontFamily="Segoe UI"
                        VerticalAlignment="Center">
                    <Run Text="{Binding PageIndex , Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}"/>
                    <Run Text="/"/> 
                    <Run Text="{Binding TotalPages , Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                    </TextBlock>

                    <Button Style="{StaticResource btnNP}"
                            Command="{Binding NextPageCommand }" 
                            IsEnabled="{Binding IsNotProcessing}"
                            Margin="0 0 0 -2"
                            CommandParameter="{Binding ElementName=root}">
                        <Icon:PackIconMaterial Kind="ChevronRight" 
                                               Height="20"
                                               VerticalAlignment="Center"/>

                    </Button>

                </StackPanel>

            </Grid>
        </Border>

    </Grid>


</UserControl>
